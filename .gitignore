# secrets — never commit these
.env
.env.local
.env.*.local
*.key

# dependencies / build
node_modules/
dist/
.vite/

# capacitor (phase 5)
android/
ios/

# editors / os
.DS_Store
Thumbs.db
.vscode/

# typescript incremental build cache
*.tsbuildinfo

# design screenshots — for looking at once, not for keeping
scripts/_reports-*.png

# supabase CLI scratch: project refs, versions, and the LOCAL stack secrets
# that `supabase start` writes. GitHub refused a push over the last of those,
# correctly — nothing under here belongs in a repository.
supabase/.temp/
